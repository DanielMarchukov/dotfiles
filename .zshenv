# Cargo/Rust environment (works on both macOS and Linux)
[[ -f "$HOME/.cargo/env" ]] && source "$HOME/.cargo/env"
# Load interactive shell config when available
[[ -f "$HOME/.zshrc" ]] && source "$HOME/.zshrc"
